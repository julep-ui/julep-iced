name: Document
on: [push, pull_request]
jobs:
  all:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
    - uses: hecrj/setup-rust-action@v2
      with:
        rust-version: nightly-2026-02-20
    - uses: actions/checkout@v2
    - name: Generate documentation
      run: |
        RUSTDOCFLAGS="--cfg docsrs" \
          cargo doc --no-deps --all-features \
          -p futures-core \
          -p toddy-iced-beacon \
          -p toddy-iced-core \
          -p toddy-iced-debug \
          -p toddy-iced-devtools \
          -p toddy-iced-futures \
          -p toddy-iced-graphics \
          -p toddy-iced-highlighter \
          -p toddy-iced-renderer \
          -p toddy-iced-runtime \
          -p toddy-iced-tiny-skia \
          -p toddy-iced-wgpu \
          -p toddy-iced-widget \
          -p toddy-iced-winit \
          -p toddy-iced-test \
          -p toddy-iced
    - name: Copy redirect file as index.html
      run: cp docs/redirect.html target/doc/index.html
    - name: Publish documentation
      if: github.ref == 'refs/heads/master'
      uses: peaceiris/actions-gh-pages@v3
      with:
        deploy_key: ${{ secrets.DOCS_DEPLOY_KEY }}
        publish_dir: ./target/doc
